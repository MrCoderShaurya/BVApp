name: Build Cordova iOS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-13

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Cordova
        run: npm install -g cordova@9

      - name: Install Dependencies
        run: npm install

      - name: Add iOS Platform
        run: cordova platform add ios@4.5.5

      - name: Prepare Project
        run: cordova prepare ios

      - name: Build iOS
        run: cordova build ios --device --verbose

      - name: Upload Build
        uses: actions/upload-artifact@v4
        with:
          name: ios-build
          path: platforms/ios/build/device
